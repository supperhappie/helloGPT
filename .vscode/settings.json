{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "cache for helloGPT",
            "database": "${workspaceFolder:helloGPT}/cache.db"
        }
    ]
}